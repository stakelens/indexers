// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:test.db"
}

model RocketPoolTVL {
  block_number BigInt @id @default(autoincrement())
  eth          BigInt
  rpl          BigInt
}
